<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projectse.aads.task_tracker"
    android:versionName="1.2"
    android:versionCode="1"
    >
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
        <meta-data
            android:name="com.google.android.apps.drive.APP_ID"
            android:value="id=826789886417-hl4sj2urrjtu7c1l2r409kufu0c2ckec.apps.googleusercontent.com" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".NotifyService.ToggleNotificationReceiver"
            android:enabled="true"
            android:exported="true"
            android:process=":notificationservice"></service>
        <receiver android:name=".NotifyService.ToggleNotificationReceiver"></receiver>

        <service
            android:name=".NotifyService.TaskTrackerBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:process=":notificationservice"></service>

        <receiver android:name=".NotifyService.TaskTrackerBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
        <activity android:name=".GoogleDrive.GoogleDriveActivity"></activity>
    </application>

</manifest>